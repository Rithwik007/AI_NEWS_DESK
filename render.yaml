services:
  - type: web
    name: ai-news-web
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node src/scripts/startAll.js
    envVars:
      - key: NODE_ENV
        value: production
